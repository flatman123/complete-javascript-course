// ternary operators


var firstName = 'john';
var age = 20

/* the following is considered if IF statement: "age >= 4 ?""
	the console.log(firstName + "can some weed") is the if BLOCK
*/
age >= 4 ? console.log(firstName + " Can smoke weed")
: console.log(firstName + "Is to young for weed"); //<<-- this is the else block


// here is another

var weed = age >= 35 ? "Go and smoke": "you\'re too young!";
console.log(weed);